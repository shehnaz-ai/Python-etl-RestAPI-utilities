pipeline:
  name: customer_etl
  environment: dev

api:
  base_url: https://dummyjson.com
  endpoint: /users
  timeout: 30

validation:
  required_columns:
    - id
    - first_name
    - last_name
    - email

output:
  path: data/processed/users_processed.csv