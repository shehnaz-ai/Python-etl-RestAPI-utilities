# Python-etl-RestAPI-utilities
I created a reusable API client to centralize HTTP request handling, timeout configuration, retry policies, and error logging. This reduces code duplication and allows multiple ETL pipelines to use a consistent ingestion component.
